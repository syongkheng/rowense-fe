
export default function copywritingEn() {

  const usernameAlphanumericLabel = 'Username can only contain letters and numbers.';
  const minimumCharacterLabel = 'Password must be greater than 8 characters.';
  const smallLetterLabel = 'Password must contain a small letter.';
  const capitalLetterLabel = 'Password must contain a capital letter.';
  const numberLabel = 'Password must contain a number.';
  const matchLabel = 'Password must match.';

  return { 
    usernameAlphanumericLabel,
    minimumCharacterLabel,
    smallLetterLabel,
    capitalLetterLabel,
    numberLabel,
    matchLabel,
  };
}